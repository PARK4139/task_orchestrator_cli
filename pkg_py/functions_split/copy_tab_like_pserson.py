from pkg_py.functions_split.press import press
from pkg_py.functions_split.press import press


def copy_tab_like_pserson():
    import random
    pk_press("ctrl", "l"
    5)
    ensure_slept(milliseconds=random.randint(a=12, b=23))
    pk_press("alt", "shift", "enter"
    5)
    # ensure_printed(f" {'%%%FOO%%%' if LTA else ''} green 새탭복사")

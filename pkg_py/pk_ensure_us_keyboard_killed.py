# -*- coding: utf-8 -*-



import traceback

if __name__ == '__main__':
    #  import deprecated_get_d_current_n_like_person, get_f_current_n, kill_us_keyboard
    #
    #, '[ TRY GUIDE ]', '[ DEBUGGING NOTE ]', '[ EXCEPTION DISCOVERED ]', D_PROJECT

    try:
        kill_us_keyboard()
    except Exception as exception:
        ensure_exception_routine_done(traceback=traceback, exception=exception)
        d_current_n = pk_deprecated_get_d_current_n_like_person()
        ensure_finally_routine_done(D_PROJECT=D_PROJECT, __file__=__file__)
        traceback_format_exc_list = traceback.format_exc().split("\n")

        ensure_printed(str_working=f'{PK_UNDERLINE}', print_color='red')
        for traceback_format_exc_str in traceback_format_exc_list:
            ensure_printed(str_working=f'{'[ EXCEPTION DISCOVERED ]'} {traceback_format_exc_str}', print_color='red')
        ensure_printed(str_working=f'{PK_UNDERLINE}', print_color='red')

        

        
